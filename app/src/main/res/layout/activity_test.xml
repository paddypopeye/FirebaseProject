<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".changeEmail"
        android:background="#f00">


    <Button
            android:layout_width="300dp"
            android:layout_height="wrap_content"
            android:id="@+id/changeEmail_btn"
            android:text="Change Email Address"
            android:layout_centerInParent="true"
            android:textSize="20sp"
            android:textColor="#fff"
            android:background="#000"
            android:textStyle="italic" android:layout_margin="10dp"/>

    <Button
            android:layout_margin="10dp"
            android:layout_centerHorizontal="true"
            android:layout_width="300dp"
            android:layout_height="wrap_content"
            android:id="@+id/submit_profile"
            android:text="Add your profile information"
            android:layout_below="@id/changeEmail_btn"
            android:textSize="20sp"
            android:textColor="#fff"
            android:background="#000"
            android:textStyle="italic" android:layout_marginTop="10dp" android:layout_marginStart="10dp"
            android:layout_marginEnd="10dp" android:layout_marginBottom="10dp"/>
</RelativeLayout>